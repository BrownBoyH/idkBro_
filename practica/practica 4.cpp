#include <bits/stdc++.h>
using namespace std;
int main()
{
    int num;
    cin >> num;
    if (num > 0)
    {
        cout << -(num) << endl;
    }
    else if (num <= 0)
    {
        cout << num << endl;
    }
    return 0;
}